name: workflow name

run-name: workflow run name

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/actions.yaml

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo hello
  say-world:
    runs-on: ubuntu-latest
    #needs: say-hello
    steps:
      - run: echo world
